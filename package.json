{
  "name": "playwright-automation-ui",
  "version": "1.0.0",
  "description": "Playwright UI automation for the DemoQA Book Store application",
  "private": true,
  
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },

  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.5",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
